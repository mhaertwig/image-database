[program:{{ application_name }}]
directory={{ application_dir }}
command={{ virtualenv_path }}/bin/gunicorn climage_project.wsgi -b 127.0.0.1:8000
autostart=true
autorestart=true
stderr_logfile=/var/log/{{ application_name }}.err.log
stdout_logfile=/var/log/{{ application_name }}.out.log
